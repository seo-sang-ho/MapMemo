spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mapmemo?serverTimezone=Asia/Seoul
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
